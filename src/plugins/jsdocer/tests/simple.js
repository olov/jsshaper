/**
 * Awesome adder function
 * @param x the first number
 * @param y the second number
 */
function adder(x, y) {
    var sum = x + y;
    var othervar = 42;
    function nested(x, z) {
        var w = 0;
        var sum = 1;
    }
    return sum;
}
